const today = new Date();
today.getMonth();

//  Typescript will raised an error when using un available methods in date
today.ekeejr();

const person = {
  age: 30,
};

person.keekjrj;

class Color {}

const red = new Color();
